import React from "react";
import { Typography, Box } from "@mui/material";

type Props = {};

function Timeline({}: Props) {
  return (
    <Box sx={{ height: "60vh", paddingY: "1rem" }}>
      <Typography variant="body1">Coming soon...</Typography>
    </Box>
  );
}

export default Timeline;
