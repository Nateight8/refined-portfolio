import React from "react";
import { Typography, Box } from "@mui/material";

type Props = {};

function Resume({}: Props) {
  return (
    <Box sx={{ minHeight: "60vh", paddingY: "1rem" }}>
      <Typography variant="body1">Coming soon...</Typography>
    </Box>
  );
}

export default Resume;
